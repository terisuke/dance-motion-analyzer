# =====================================
# Dance Motion Analyzer - Environment Variables Template
# =====================================
# 使い方:
# 1. このファイルを .env にコピー
# 2. 必要な値を設定
# 3. .env は絶対にGitにコミットしない

# === 🔴 必須設定（これらは必ず設定してください） ===

# Gemini API Keys
# 取得方法: https://makersuite.google.com/app/apikey
# ⚠️ 重要: APIキーは絶対にコードに直接書かない
VITE_GEMINI_API_KEY=  # Frontend用（Phase 2以降は不要）
GEMINI_API_KEY=       # Backend用

# === 🟡 Backend設定（Docker Composeのデフォルト値） ===

# PostgreSQL Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=dance_analyzer
DATABASE_URL=postgresql://postgres:postgres@db:5432/dance_analyzer

# Redis Cache
REDIS_URL=redis://redis:6379/0

# Security (本番環境では必ず変更)
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# === 🟢 Frontend設定 ===

# API Backend URL
VITE_API_URL=http://localhost:8000

# Feature Flags (オプション)
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG=false

# === 🔵 Docker設定 ===

# Project Name
COMPOSE_PROJECT_NAME=dance-motion-analyzer

# === 📝 メモ ===
# - Phase 1: Frontend は Gemini API を直接呼び出し（VITE_GEMINI_API_KEY 使用）
# - Phase 2: Backend 経由に移行（GEMINI_API_KEY のみ使用）
# - 本番環境: すべてのパスワードとキーを強力なものに変更